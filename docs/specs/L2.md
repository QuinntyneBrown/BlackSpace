# L2 Requirements — blackspace.ca

---

## L1-1: Hero Section

### L2-1.1: Desktop Hero Layout (≥1024px)
The hero section occupies the full viewport width and height. Headline "Black Canadians in Space & Defence" is large (48–64px), centered. Subheadline sits below at 18–20px. Two buttons horizontally aligned: "Join the Community" (primary, filled) and "Learn More" (secondary, outlined). Background is deep navy/black with subtle gradient or star-field texture.
**Acceptance Criteria:** Desktop frame shows a full-width hero with centered headline, subheadline, and two horizontally-aligned CTA buttons on a dark space-themed background. All content is above the fold at 1440×900.

### L2-1.2: Tablet Hero Layout (768–1023px)
Same content as desktop but headline scales to 36–44px. Buttons remain horizontal. Padding adjusts for narrower viewport.
**Acceptance Criteria:** Tablet (768px) frame shows the hero with appropriately scaled typography and maintained horizontal button layout.

### L2-1.3: Mobile Hero Layout (<768px)
Headline scales to 28–36px. Subheadline to 16px. Buttons stack vertically, full-width. Content remains centered and above the fold on a 375px-wide screen.
**Acceptance Criteria:** Mobile (375px) frame shows stacked buttons, scaled-down text, and all hero content visible without scrolling.

---

## L1-2: Problem Section

### L2-2.1: Desktop Problem Layout
Section with a heading ("Why This Exists" or similar), 3–4 short paragraphs or bold statement blocks with supporting text. Optional stat callout (e.g., "0 Black Canadian astronauts to date"). Max-width container (~800px) for readability. Dark background with contrasting text.
**Acceptance Criteria:** Desktop frame shows a readable, conversational problem section with bold statements, supporting text, and at least one stat callout, centered within a max-width container.

### L2-2.2: Mobile Problem Layout
Same content, full-width with appropriate padding (16–24px). Font sizes scale down. Stat callout remains visually prominent.
**Acceptance Criteria:** Mobile frame shows the problem section with readable text, proper padding, and a visible stat callout without horizontal overflow.

---

## L1-3: Who It's For Section

### L2-3.1: Desktop Audience Cards
Section heading + 5 cards in a grid (3+2 or flexible grid). Each card has an icon, role title, and one-line description. Cards have subtle borders or background differentiation.
**Acceptance Criteria:** Desktop frame shows 5 audience cards in a multi-column grid with icons, titles, and one-line descriptions.

### L2-3.2: Mobile Audience Cards
Cards stack vertically, full-width. Each card maintains icon, title, and description.
**Acceptance Criteria:** Mobile frame shows all 5 cards stacked vertically with no horizontal overflow and readable text.

---

## L1-4: What We Do Section

### L2-4.1: Desktop Pillars Layout
Section heading + three equal-width columns for Networking, Visibility, Pipeline. Each column has an icon, pillar name, and 2–3 sentence description grounded in the mission: Networking — building professional networks; Visibility — creating visibility for Black Canadians already doing the work; Pipeline — opening pathways for the next generation to enter and thrive.
**Acceptance Criteria:** Desktop frame shows three equal columns with icons, pillar names (Networking, Visibility, Pipeline), and credibility-first descriptions.

### L2-4.2: Mobile Pillars Layout
Columns stack vertically. Each pillar (Networking, Visibility, Pipeline) is a full-width card with mission-aligned descriptions.
**Acceptance Criteria:** Mobile frame shows all three pillars (Networking, Visibility, Pipeline) stacked vertically as full-width cards with grounded, credibility-first descriptions.

---

## L1-5: Signup / Join Form

### L2-5.1: Desktop Form Layout
Section heading ("Join Black Space Canada"). Form on a slightly lighter panel/card against the dark background. Fields: Full Name (required), Email (required), Current Role/Title, Organization, How did you hear about us? (dropdown). "Join Black Space Canada" button. Reassurance text below.
**Acceptance Criteria:** Desktop frame shows a form card with all 5 fields, a primary CTA button, and reassurance text on a distinct panel.

### L2-5.2: Mobile Form Layout
Form fills the screen width with appropriate padding. Fields stack vertically. Button is full-width.
**Acceptance Criteria:** Mobile frame shows the form with stacked fields, full-width button, and no horizontal overflow.

### L2-5.3: Form Success State
After submission, the form area transforms to show: "You're in. Welcome to the community." with a checkmark icon and a brief message about what to expect next.
**Acceptance Criteria:** A separate frame shows the post-submission success state replacing the form content with a welcome message and checkmark.

---

## L1-6: About the Founder

### L2-6.1: Desktop Founder Layout
Small section with photo placeholder (circle), name "Quinn Brown", title, 1–2 sentence bio, and LinkedIn icon/link. Horizontal layout: photo left, text right.
**Acceptance Criteria:** Desktop frame shows a compact founder section with circular photo, name, title, bio, and LinkedIn link in a horizontal layout.

### L2-6.2: Mobile Founder Layout
Photo centered above text. Name, title, bio, and link stack below.
**Acceptance Criteria:** Mobile frame shows the founder section with centered photo above stacked text content.

---

## L1-7: Footer

### L2-7.1: Desktop Footer
Dark background. Single row or two rows: contact email, LinkedIn link, disclaimer text, copyright "© 2026 Black Space Canada". Minimal and clean.
**Acceptance Criteria:** Desktop frame shows a dark footer with email, LinkedIn link, disclaimer, and copyright in a clean layout.

### L2-7.2: Mobile Footer
Content stacks vertically, centered. Same elements.
**Acceptance Criteria:** Mobile frame shows footer content stacked and centered.

---

## L1-8: Navigation & Mobile Menu

### L2-8.1: Desktop Navigation Bar
Fixed/sticky top bar. Left: "Black Space Canada" wordmark. Right: section links (About, What We Do, Join) and a "Join" CTA button. Dark background, translucent or solid.
**Acceptance Criteria:** Desktop frame shows a fixed nav bar with wordmark on left and navigation links + CTA on right.

### L2-8.2: Mobile Navigation Bar
Fixed top bar. Left: wordmark (shortened if needed). Right: hamburger menu icon.
**Acceptance Criteria:** Mobile frame shows a compact nav bar with wordmark and hamburger icon.

### L2-8.3: Mobile Menu Overlay
Full-screen or slide-in overlay when hamburger is tapped. Shows section links vertically with a "Join the Community" CTA. Close (X) button in the top right.
**Acceptance Criteria:** A separate frame shows the mobile menu overlay with vertical links, CTA button, and close button.

---

## L1-9: Notifications, Alerts, Dialogs & Popups

### L2-9.1: Form Validation Errors
Inline error states on form fields: red border, error message below the field (e.g., "Please enter your name", "Please enter a valid email"). Top-of-form summary banner for multiple errors.
**Acceptance Criteria:** A frame shows the signup form with at least two fields in error state — red borders, inline error messages, and a summary error banner at the top.

### L2-9.2: Success Toast Notification
A toast notification that appears at the top or bottom of the screen confirming form submission: "You're in! Check your email for next steps." Auto-dismisses after a few seconds. Green accent with checkmark icon.
**Acceptance Criteria:** A frame shows a toast notification overlaying the page with success message, checkmark icon, green accent, and dismiss/close button.

### L2-9.3: Already Registered Alert
An inline alert or modal that appears if an email is already registered: "This email is already part of the community. Check your inbox for your welcome email or contact hello@blackspace.ca." Amber/warning accent.
**Acceptance Criteria:** A frame shows an alert dialog or inline banner with the already-registered message, amber/warning styling, and a dismiss action.

### L2-9.4: Cookie / Privacy Notice
A bottom banner or bar informing users about analytics/cookies: "We use analytics to improve your experience. No personal data is sold." with "Accept" and "Learn More" buttons. Non-intrusive.
**Acceptance Criteria:** A frame shows a bottom-anchored cookie banner with message text, Accept button, and Learn More link.

### L2-9.5: Generic Confirmation Dialog
A centered modal dialog with title, body text, and two buttons (Confirm / Cancel). Semi-transparent backdrop. Used for actions like unsubscribe confirmation.
**Acceptance Criteria:** A frame shows a centered modal dialog with backdrop overlay, title, body text, and two action buttons.

---

# Backend API Requirements

## L1-10: .NET Solution Structure & Configuration

### L2-10.1: Solution and Project Structure
Create a .NET 9 solution at `src/BlackSpace.Api/` with four projects: `BlackSpace.Api` (ASP.NET Core Web API using Minimal APIs), `BlackSpace.Domain` (class library for entities, DTOs, interfaces), `BlackSpace.Infrastructure` (class library for EF Core DbContext, repositories, email service), and `BlackSpace.Tests` (xUnit test project). All projects target .NET 9.
**Acceptance Criteria:** `dotnet build` succeeds on the solution. The solution contains 4 projects with correct references: Api → Domain + Infrastructure, Infrastructure → Domain, Tests → Api + Domain + Infrastructure.

### L2-10.2: Dependency Injection & Configuration
Configure DI for all services (DbContext, repositories, email service) in Program.cs. Use `appsettings.json` and `appsettings.Development.json` for environment-specific config including database connection string, SMTP settings, CORS origins, and LinkedIn group URL.
**Acceptance Criteria:** The application starts without errors. All services resolve correctly from the DI container. Configuration values are read from appsettings files.

### L2-10.3: CORS Configuration
Configure CORS to allow the Angular frontend (localhost:4200 in development, configurable production origin) to make requests. Allow GET, POST, OPTIONS methods and standard headers.
**Acceptance Criteria:** An integration test confirms that CORS headers are present on API responses for allowed origins. Requests from disallowed origins are rejected.

---

## L1-11: Member Signup API

### L2-11.1: Member Entity and DTOs
Create a `Member` entity with properties: Id (Guid), FullName (string, required), Email (string, required, unique), RoleTitle (string, nullable), Organization (string, nullable), ReferralSource (string, nullable), CreatedAtUtc (DateTime), and IpAddress (string, nullable). Create `CreateMemberRequest` and `MemberResponse` DTOs.
**Acceptance Criteria:** The Member entity maps correctly to the database. DTOs serialize/deserialize correctly in JSON. A unit test validates the entity-to-DTO mapping.

### L2-11.2: POST /api/members Endpoint
Implement a POST /api/members endpoint that accepts `CreateMemberRequest` JSON body, validates it using FluentValidation (FullName required, max 200 chars; Email required, valid format, max 254 chars; optional fields max 200 chars), creates a Member record, and returns 201 Created with the `MemberResponse` body and a Location header.
**Acceptance Criteria:** An integration test POSTs valid member data and receives 201 with correct response body. The member exists in the database after the call.

### L2-11.3: Duplicate Email Detection
If a POST /api/members request contains an email that already exists in the database (case-insensitive comparison), return 409 Conflict with a JSON error body: `{"error": "already_registered", "message": "This email is already part of the community."}`.
**Acceptance Criteria:** An integration test POSTs the same email twice. The first returns 201, the second returns 409 with the correct error body.

### L2-11.4: Validation Error Responses
If validation fails, return 422 Unprocessable Entity with a JSON body: `{"error": "validation_failed", "message": "Please fix the errors below.", "errors": {"fieldName": ["error message"]}}`. The response follows RFC 7807 problem details pattern.
**Acceptance Criteria:** An integration test POSTs invalid data (empty name, invalid email) and receives 422 with field-level error messages.

---

## L1-12: Email Service Integration

### L2-12.1: IEmailService Interface and Welcome Email
Define `IEmailService` with a `SendWelcomeEmailAsync(Member member)` method. Implement `SmtpEmailService` that sends a templated HTML welcome email via SMTP with subject "Welcome to Black Space Canada", containing the member's name, LinkedIn group link, and next meetup details. Implement `ConsoleEmailService` for development that logs email content to the console.
**Acceptance Criteria:** A unit test with a mock verifies that `SendWelcomeEmailAsync` is called after successful member creation. The ConsoleEmailService logs the email subject and recipient.

### L2-12.2: Email Dispatch on Signup
After a successful member creation in POST /api/members, dispatch the welcome email asynchronously (fire-and-forget with error logging). Email failures must not cause the signup to fail — the member is still created and 201 is returned.
**Acceptance Criteria:** An integration test confirms that a member is created (201) even when email sending throws an exception. The error is logged.

---

## L1-13: Site Content API

### L2-13.1: GET /api/content/stats
Return community statistics: `{"memberCount": <int>, "nextMeetupDate": "<ISO date or null>"}`. Member count is queried from the database.
**Acceptance Criteria:** An integration test creates 3 members, then GETs /api/content/stats and verifies memberCount is 3.

### L2-13.2: GET /api/content/referral-sources
Return the list of referral source options for the signup form dropdown: `["LinkedIn", "Twitter/X", "A friend or colleague", "CSA/DND event", "Google search", "Other"]`.
**Acceptance Criteria:** An integration test GETs the endpoint and receives the expected array of strings.

---

## L1-14: Health & Observability

### L2-14.1: Health Check Endpoint
Implement GET /api/health using ASP.NET Core health checks. Include a database connectivity check. Return 200 with `{"status": "Healthy"}` when all checks pass, 503 when unhealthy.
**Acceptance Criteria:** An integration test GETs /api/health and receives 200 with status "Healthy".

### L2-14.2: Structured Logging with Serilog
Configure Serilog with console sink and structured JSON output. Log all incoming requests (method, path, status code, duration). Log member signups and email dispatch results at Information level. Log errors at Error level.
**Acceptance Criteria:** Application starts with Serilog configured. A test verifies that Serilog is registered as the logging provider.

### L2-14.3: Swagger/OpenAPI Documentation
Configure Swagger UI at /swagger and OpenAPI spec at /swagger/v1/swagger.json. All endpoints have summary descriptions and response type annotations.
**Acceptance Criteria:** An integration test GETs /swagger/v1/swagger.json and receives a valid OpenAPI document containing all endpoint paths.

---

## L1-15: API Integration Tests

### L2-15.1: Test Infrastructure
Create a `CustomWebApplicationFactory<T>` that configures an in-memory SQLite database, replaces the email service with a test double, and provides helper methods for common test operations (creating members, asserting responses).
**Acceptance Criteria:** The test factory creates a working test server. A simple test GETs /api/health and receives 200.

### L2-15.2: Full Endpoint Coverage
Write integration tests covering: successful member creation, duplicate email rejection, validation errors (missing name, invalid email, field too long), stats endpoint accuracy, referral sources list, health endpoint, and Swagger availability.
**Acceptance Criteria:** All tests pass. `dotnet test` reports 0 failures. Every API endpoint has at least one test.

---

# Frontend Implementation Requirements

## L1-16: Components Library

### L2-16.1: ButtonPrimary Component
Standalone Angular component with inputs for label text, disabled state, and loading state. Renders a filled blue (#4F9CF7) button with white text, Inter font 16px 600 weight, 8px corner radius, 16px/32px padding. Emits a clicked output event.
**Acceptance Criteria:** Unit test renders component with custom label, verifies click event emission, verifies disabled state prevents clicks, verifies loading state shows spinner indicator. Close to 100% line coverage.

### L2-16.2: ButtonSecondary Component
Standalone Angular component matching ButtonPrimary API but styled as outlined: transparent background with #FFFFFF30 border, white text, 8px corner radius. Same padding and typography.
**Acceptance Criteria:** Unit test renders component, verifies outline styling, click events, and disabled behavior. Close to 100% line coverage.

### L2-16.3: FormInput Component
Standalone Angular component with inputs for label, placeholder, error message, required flag, and field type (text/email/select). Implements ControlValueAccessor for reactive forms integration. Dark field styling: #0F1019 background, #2A2A3A border, 8px corner radius, 14px/16px padding. Error state shows #FF3B30 border and error text. Label is Inter 13px 500 weight #8A8A9A.
**Acceptance Criteria:** Unit test verifies label rendering, placeholder, value binding via ngModel/formControl, error state display with red border, and required validation feedback. Close to 100% line coverage.

### L2-16.4: AudienceCard Component
Standalone Angular component with inputs for icon name (Lucide icon), title, and description. Dark card: #0F1019 background, #1A1A2E border, 12px corner radius, 24px padding. Icon is #4F9CF7 28x28. Title is Sora 17px 700 weight white. Description is Inter 14px normal #8A8A9A with 1.5 line height.
**Acceptance Criteria:** Unit test verifies icon, title, and description rendering with correct structure. Close to 100% line coverage.

### L2-16.5: PillarCard Component
Standalone Angular component with inputs for icon name, title, and description. Dark card: #0F1019 background, #1A1A2E border, 12px corner radius, 32px padding. Icon container: 48x48 #4F9CF720 background with 12px corner radius, icon #4F9CF7 24x24. Title is Sora 22px 700 weight white. Description is Inter 15px normal #8A8A9A with 1.6 line height.
**Acceptance Criteria:** Unit test verifies icon container, title, and description rendering. Close to 100% line coverage.

### L2-16.6: SectionHeader Component
Standalone Angular component with inputs for label (small uppercase text), headline, and optional subtext. Label is Inter 11px 700 weight #4F9CF7, letter-spacing 3px. Headline is Sora 40px 700 weight white, letter-spacing -1.5px. All centered.
**Acceptance Criteria:** Unit test verifies label, headline, and optional subtext rendering with correct typography classes. Close to 100% line coverage.

### L2-16.7: Toast Component
Standalone Angular component with inputs for message, description, type (success/warning/error), and visible state. Success: green (#32D74B) accent with check icon, #0D2818 background. Warning: amber (#FF9900) with alert icon, #2A1E08 background. Includes close button (Lucide x icon). Auto-dismiss timer configurable.
**Acceptance Criteria:** Unit test verifies success and warning variants render correct colors and icons, close button emits event, auto-dismiss timer works. Close to 100% line coverage.

### L2-16.8: AlertBanner Component
Standalone Angular component with inputs for icon, message, and type (error/warning/info). Error type: #FF3B30 icon/text on #FF3B3015 background with #FF3B3040 border. Used for form validation summary banners.
**Acceptance Criteria:** Unit test verifies error variant renders with correct styling, icon, and message. Close to 100% line coverage.

### L2-16.9: CookieBanner Component
Standalone Angular component displaying cookie/privacy notice at bottom. #0D0E18 background, #1A1A2E border, 12px corner radius. Contains message text, Accept button (ButtonPrimary), Learn More button (ButtonSecondary). Emits accept and learnMore events.
**Acceptance Criteria:** Unit test verifies message text, both buttons render, and events emit on click. Close to 100% line coverage.

### L2-16.10: ConfirmDialog Component
Standalone Angular component with inputs for title, body text, confirmLabel, cancelLabel, and confirmVariant (danger/primary). Centered modal with #00000080 backdrop, #0D0E18 card, #1A1A2E border, 16px corner radius, 32px padding. Emits confirm and cancel events.
**Acceptance Criteria:** Unit test verifies title, body, buttons render. Confirm/cancel events fire. Backdrop click emits cancel. Close to 100% line coverage.

### L2-16.11: SuccessState Component
Standalone Angular component showing post-submission success. Checkmark icon in #32D74B15 circle with #32D74B30 border. Title "You're in. Welcome to the community." in Sora 24px 700 weight. Description in Inter 15px #8A8A9A. Blue accent line divider.
**Acceptance Criteria:** Unit test verifies icon, title, and description render with correct structure. Close to 100% line coverage.

### L2-16.12: MobileMenuOverlay Component
Standalone Angular component with inputs for isOpen state and navigation links. Full-screen dark overlay (#07080FF5). Shows logo, close button, vertical navigation links (About, Who It's For, What We Do, Founder), and Join CTA button. Emits close and navigate events.
**Acceptance Criteria:** Unit test verifies overlay visibility toggles, links render, close button works, navigation events emit. Close to 100% line coverage.

---

## L1-17: API Library

### L2-17.1: TypeScript Models
Create TypeScript interfaces in the api library matching backend DTOs: CreateMemberRequest (fullName, email, roleTitle?, organization?, referralSource?), MemberResponse (id, fullName, email, roleTitle?, organization?, createdAtUtc), ContentStats (memberCount, nextMeetupDate?), ValidationErrorResponse (error, message, errors), ConflictErrorResponse (error, message), and ReferralSource string array type.
**Acceptance Criteria:** Unit test imports all interfaces and verifies they compile. Type-check test creates objects matching each interface.

### L2-17.2: MemberService
Injectable Angular service using HttpClient. Methods: createMember(request: CreateMemberRequest): Observable<MemberResponse> — POSTs to /api/members. getMember(id: string): Observable<MemberResponse> — GETs /api/members/{id}. Base URL configurable via injection token.
**Acceptance Criteria:** Unit test with HttpTestingController verifies createMember sends correct POST body and URL, getMember sends correct GET URL. Tests verify error handling for 409, 422, and network errors. Close to 100% line coverage.

### L2-17.3: ContentService
Injectable Angular service using HttpClient. Methods: getStats(): Observable<ContentStats>, getReferralSources(): Observable<string[]>. Base URL configurable.
**Acceptance Criteria:** Unit test verifies correct HTTP calls and response mapping for both methods. Close to 100% line coverage.

### L2-17.4: HealthService
Injectable Angular service using HttpClient. Method: checkHealth(): Observable<{status: string}>. GETs /api/health.
**Acceptance Criteria:** Unit test verifies GET call to correct URL and response mapping. Close to 100% line coverage.

### L2-17.5: API Configuration
Provide an InjectionToken API_BASE_URL with default value 'http://localhost:5000'. All services inject this token for base URL. Export a provideApi(baseUrl?: string) function for app configuration.
**Acceptance Criteria:** Unit test verifies default URL is used when no override. Test verifies custom URL when provided. Close to 100% line coverage.

---

## L1-18: Domain Library

### L2-18.1: SignupFormContainer Component
Standalone Angular component that manages the entire signup form lifecycle. Uses FormInput components from components library for all 5 fields with reactive forms (FormGroup with validators). Calls MemberService.createMember on submit. Handles validation errors (maps API 422 errors to form fields), duplicate email (409 shows AlertBanner), network errors, and success state (switches to SuccessState component). Shows loading state on button during submission.
**Acceptance Criteria:** Unit test verifies form renders 5 fields, validates required fields, submits to API, handles 201/409/422 responses correctly, shows success state on 201, shows errors on 422, shows alert on 409. Close to 100% line coverage.

### L2-18.2: LandingPage Component
Standalone Angular component that composes all page sections in order: NavBar, Hero, Problem, WhoItsFor, Audience cards, WhatWeDo, Pillar cards, Signup (SignupFormContainer), Founder, Footer. Uses SectionHeader for each section. Fetches referral sources via ContentService on init. Fetches stats via ContentService for the Problem section stat callout.
**Acceptance Criteria:** Unit test verifies all sections render in correct order, ContentService calls are made on init. Close to 100% line coverage.

### L2-18.3: NavigationContainer Component
Standalone Angular component managing the sticky navigation bar state. Tracks scroll position to add/remove background opacity. Manages mobile menu open/close state. Handles smooth-scroll navigation to page sections via anchor IDs. On mobile (<768px) shows hamburger icon that opens MobileMenuOverlay.
**Acceptance Criteria:** Unit test verifies nav renders, mobile menu toggle works, navigation events scroll to sections. Close to 100% line coverage.

### L2-18.4: CookieConsentManager Component
Standalone Angular component that shows CookieBanner on first visit (checks localStorage). On accept, stores consent in localStorage and hides banner. Learn More opens a simple info section or link.
**Acceptance Criteria:** Unit test verifies banner shows when no localStorage key, hides after accept, does not show on revisit. Close to 100% line coverage.

---

## L1-19: BlackSpace Application

### L2-19.1: Application Shell and Routing
Configure the main app component with NavigationContainer, CookieConsentManager, and RouterOutlet. Single route "/" loads LandingPage. Configure provideHttpClient() and provideApi() in app config. Global styles import Sora and Inter fonts from Google Fonts. Set body background to #07080F, default text color #FFFFFF, font-family Inter.
**Acceptance Criteria:** `ng build blackspace` succeeds. Application renders the landing page at root route with correct fonts and colors.

### L2-19.2: Global Styles and Theming
Implement global SCSS with CSS custom properties matching the design: --color-bg: #07080F, --color-surface: #0F1019, --color-surface-raised: #0D0E18, --color-border: #1A1A2E, --color-primary: #4F9CF7, --color-text: #FFFFFF, --color-text-secondary: #8A8A9A, --color-text-muted: #52526A, --color-success: #32D74B, --color-warning: #FF9900, --color-error: #FF3B30. Import Sora (700 weight) and Inter (400, 500, 600, 700 weights) from Google Fonts. Include Lucide icons via CDN or npm package. Reset/normalize styles.
**Acceptance Criteria:** Application renders with correct dark theme colors, Sora headings, Inter body text, and Lucide icons are available.

### L2-19.3: Responsive Layout Implementation
All sections implement three breakpoints matching the design: Desktop (≥1024px, 1440px reference), Tablet (768–1023px), Mobile (<768px). Hero padding, font sizes, card grids, and form widths all adapt per design frames. Desktop audience grid: 3+2 columns. Tablet: 2+2+1. Mobile: stacked. Desktop pillar grid: 3 columns. Tablet: stacked. Mobile: stacked.
**Acceptance Criteria:** Application renders correctly at 1440px, 768px, and 375px widths matching the design layouts.

### L2-19.4: Playwright E2E Test Infrastructure
Set up Playwright in the BlackSpace.Web project with page object model. Create page objects: LandingPagePO (with methods for each section), SignupFormPO (fill, submit, verify errors/success), NavigationPO (click links, toggle mobile menu), CookieBannerPO (accept, dismiss). Configure for Chrome with 1440px, 768px, and 375px viewports.
**Acceptance Criteria:** Playwright test infrastructure runs. A smoke test navigates to localhost:4200 and verifies the page loads.

### L2-19.5: Playwright Font and Icon Tests
Write Playwright tests that verify: Sora font loads and is applied to headings (computed fontFamily check), Inter font loads and is applied to body text, Lucide icons render in the navigation, audience cards, pillar cards, and toast components (icon elements visible and have correct dimensions).
**Acceptance Criteria:** All font and icon Playwright tests pass. Tests fail if fonts or icons are missing.

### L2-19.6: Playwright Visual Screenshot Tests
Write Playwright tests that take screenshots at each breakpoint (1440px, 768px, 375px) for: full page, hero section, audience cards section, pillar cards section, signup form, footer, and navigation. Screenshots are saved to a test-results directory for manual comparison against the design.
**Acceptance Criteria:** All screenshot tests pass. Screenshot files are generated for each section at each breakpoint.

### L2-19.7: Playwright Functional E2E Tests
Write Playwright tests for: form submission with valid data shows success state, form submission with empty required fields shows validation errors, navigation links scroll to correct sections, mobile menu opens and closes, cookie banner appears and can be accepted, responsive layout changes at breakpoints.
**Acceptance Criteria:** All functional Playwright tests pass.
